#Tendo a base e a altura de um triângulo,
#calcule sua área por meio da fórmula: area = (base x altura)/2.]

print('Bem vindo ao calculo de área de um triangulo')

m1= float(input('Digite o valor da base do triangulo: '))
m2= float(input('Digite o valor da altura do triangulo: '))

area= ( m1 * m2 )/2

print('Para calcular a área precisamos multiplicar o valor da base'
      ' vezes altura e dividir por 2, então teremos o resultado da área ')
print(f'O valor da área é = {(m1*m2)/2}')
